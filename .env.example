BACKEND_PORT=3000
NODE_ENV=development

REDIS_PASSWORD=your_redis_password

# LocalStack Pro auth token (get from https://app.localstack.cloud)
LOCALSTACK_AUTH_TOKEN=your_localstack_auth_token

# Python service environment ('development' or 'production') and port
PYTHON_ENV=development
PYTHON_PORT=8000
